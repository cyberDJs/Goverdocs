# R7 PR Evidence Contract Dogfood Probe

This file exists only to exercise the canonical R7 pull-request evidence path.

It is intentionally non-production and must not be merged. The probe succeeds when GOVERDOCS recognizes exact-HEAD-bound `Change rationale` and `Affected scope` declarations from the pull request body and no longer reports those two evidence requirements as missing.
